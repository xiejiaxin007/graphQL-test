{
  "name": "graphQL",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@babel/cli": "^7.12.16",
    "@babel/core": "^7.12.16",
    "@babel/node": "^7.12.16",
    "@babel/preset-env": "^7.12.16",
    "axios": "^0.21.1",
    "express": "^4.17.1",
    "express-graphql": "^0.12.0",
    "graphql": "^15.5.0"
  },
  "scripts": {
    "web": "nodemon webapp.js --exec babel-node --presets @babel/preset-env",
    "dev": "nodemon index.js --exec babel-node --presets @babel/preset-env",
    "json-server": "json-server --watch db.json --port 3300",
    "json-server-mock": "json-server --watch mock.json --port 3400",
    "dev-mock": "nodemon indexMock.js --exec babel-node --presets @babel/preset-env"
  }
}
