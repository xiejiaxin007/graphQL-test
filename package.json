{
  "name": "graphQL",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.1",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-stage-0": "^6.24.1",
    "express": "^4.17.1",
    "express-graphql": "^0.12.0",
    "graphql": "^15.5.0"
  },
  "scripts": {
    "dev": "nodemon index.js --exec babel-node --presets env stage-0",
    "json-server": "json-server --watch db.json --port 3300",
    "json-server-mock": "json-server --watch mock.json --port 3400",
    "dev-mock": "nodemon indexMock.js --exec babel-node --presets env stage-0"
  }
}
